import styled from 'styled-components';

const Form = styled.form`
  display: flex;
  flex-direction: column;
  width: 33%;
`;

const FormButtonGroup = styled.div`
  display: flex;
  justify-content: flex-end;
  margin: 0;
  padding: 0;
`;

export { Form, FormButtonGroup };
